#include <stdio.h>
#include "sort.h"


int main() {
  int array[] = {3,5,2,6,4,1,7};
  int length = 7;//sizeof(array)/sizeof(array[0]);
  for(int i =0;i<length;i++){
    printf("%d",array[i]);
  }
  printf("\n");
  Sort(array,length);
  for(int i=0;i<length;i++){
    printf("%d",array[i]);
  }

}
